# Initialize

* git init
* git remote add origin <"clone">

# Pushing Files

* git add . // untracked files added
* git commit -am '...' // tracked files modified
* git push

# Deleting Files

* git rm -rf filename
* git push